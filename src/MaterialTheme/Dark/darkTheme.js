import {createTheme} from "@mui/material";

export const darkButton = createTheme({
        palette: {
            dark: {
                main: '#000000',
                darker: '#1c1c1c',
            },
        },
    })